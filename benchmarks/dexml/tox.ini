[tox]
envlist = py27

[testenv]
deps= coverage
commands = coverage erase
           coverage run setup.py test
           coverage report --include=*dexml* --omit=*test*

[testenv:pypy-c]
deps= 
commands = pypy-c setup.py test
