static void *opcode_targets[256] = {
	&&TARGET_NULLBYTE,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_MARK,
	&&TARGET_EMPTY_TUPLE,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_STOP,
	&&_unknown_opcode,
	&&TARGET_POP,
	&&TARGET_POP_MARK,
	&&TARGET_DUP,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_FLOAT,
	&&TARGET_BINFLOAT,
	&&_unknown_opcode,
	&&TARGET_INT,
	&&TARGET_BININT,
	&&TARGET_BININT1,
	&&TARGET_LONG,
	&&TARGET_BININT2,
	&&TARGET_NONE,
	&&_unknown_opcode,
	&&TARGET_PERSID,
	&&TARGET_BINPERSID,
	&&TARGET_REDUCE,
	&&TARGET_STRING,
	&&TARGET_BINSTRING,
	&&TARGET_SHORT_BINSTRING,
	&&TARGET_UNICODE,
	&&_unknown_opcode,
	&&TARGET_BINUNICODE,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_EMPTY_LIST,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_APPEND,
	&&TARGET_BUILD,
	&&TARGET_GLOBAL,
	&&TARGET_DICT,
	&&TARGET_APPENDS,
	&&_unknown_opcode,
	&&TARGET_GET,
	&&TARGET_BINGET,
	&&TARGET_INST,
	&&TARGET_LONG_BINGET,
	&&_unknown_opcode,
	&&TARGET_LIST,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_OBJ,
	&&TARGET_PUT,
	&&TARGET_BINPUT,
	&&TARGET_LONG_BINPUT,
	&&TARGET_SETITEM,
	&&TARGET_TUPLE,
	&&TARGET_SETITEMS,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_EMPTY_DICT,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&TARGET_PROTO,
	&&TARGET_NEWOBJ,
	&&TARGET_EXT1,
	&&TARGET_EXT2,
	&&TARGET_EXT4,
	&&TARGET_TUPLE1,
	&&TARGET_TUPLE2,
	&&TARGET_TUPLE3,
	&&TARGET_NEWTRUE,
	&&TARGET_NEWFALSE,
	&&TARGET_LONG1,
	&&TARGET_LONG4,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode,
	&&_unknown_opcode
};
